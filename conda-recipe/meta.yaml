package:
    name: app_config
    version: 1.0.11
source:
    # git_tag: v0.9.7
    # git_url: https://github.psi.ch/scm/cos/pylauncher.git
    path: ..
requirements:
    build:
        - setuptools

build:
  entry_points:
    - app_config = app_config.app_config:main

about:
    home: https://git.psi.ch/git_tools/git_workflow_tools
    summary: "Configuration management tools to support configuration change workflows"
